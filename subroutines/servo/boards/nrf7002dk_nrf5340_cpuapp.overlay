
/ {
	servo: servo {
		compatible = "pwm-servo";
		pwms = <&sw_pwm 0 PWM_MSEC(20) PWM_POLARITY_NORMAL>;
		min-pulse = <PWM_USEC(900)>;
		max-pulse = <PWM_USEC(2100)>;
	};
};

&sw_pwm {
    status = "okay";
    clock-prescaler = <3>;
    channel-gpios = <&gpio0 4 GPIO_ACTIVE_HIGH>;
};