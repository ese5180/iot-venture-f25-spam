name: Build nRF7002DK Firmware

on:
  push:
    branches: [main, master, develop]
    paths-ignore:
      - 'docs/**'
      - '**.md'
  pull_request:
  workflow_dispatch:

env:
  NCS_VERSION: v3.0.2
  BOARD: nrf7002dk/nrf5340/cpuapp
  DOCKER_IMAGE: ghcr.io/zephyrproject-rtos/ci:v0.27.4

jobs:
  # ============================================================
  # STEP 0: Pre-pull Docker image ONCE (shared by all builds)
  # ============================================================
  prepare:
    name: Prepare Environment
    runs-on: ubuntu-24.04
    outputs:
      cache-key: ${{ steps.cache-key.outputs.key }}
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          path: project

      - name: 🔑 Generate Cache Key
        id: cache-key
        run: |
          KEY="west-modules-${{ env.NCS_VERSION }}-${{ hashFiles('project/west.yml') }}"
          echo "key=$KEY" >> $GITHUB_OUTPUT
          echo "Cache key: $KEY"

      - name: 🧹 Free Disk Space
        run: |
          echo "Disk space before cleanup:"
          df -h
          
          # Remove unnecessary tools to free ~10GB
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo apt-get clean
          
          echo "Disk space after cleanup:"
          df -h

      - name: 🐳 Pull Docker Image
        run: |
          echo "Pulling Docker image (this is shared across all build jobs)..."
          docker pull ${{ env.DOCKER_IMAGE }}
          echo "✅ Docker image cached for subsequent jobs"

  # ============================================================
  # STEP 1: Build all apps (depends on prepare job)
  # ============================================================
  build:
    name: Build ${{ matrix.app.name }}
    runs-on: ubuntu-24.04
    needs: prepare
    timeout-minutes: 60
    
    strategy:
      fail-fast: false
      matrix:
        app:
          - name: "Root Main App"
            path: ""
            build_name: "root-app"
          
          - name: "Motion Sensor"
            path: "subroutines/Motion_sensor"
            build_name: "motion-sensor"
          
          - name: "BLE NUS"
            path: "subroutines/ble_nus"
            build_name: "ble-nus"
          
          - name: "Servo"
            path: "subroutines/servo"
            build_name: "servo"

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          path: project
          fetch-depth: 0

      - name: 🧹 Free Disk Space
        run: |
          echo "Disk space before cleanup:"
          df -h
          
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo apt-get clean
          docker system prune -af
          
          echo "Disk space after cleanup:"
          df -h

      - name: 💾 Cache West Modules
        id: cache-west
        uses: actions/cache@v4
        with:
          path: |
            modules
            zephyr
            nrf
            bootloader
            tools
          key: ${{ needs.prepare.outputs.cache-key }}
          restore-keys: |
            west-modules-${{ env.NCS_VERSION }}-
            west-modules-

      - name: 🔧 Initialize & Build
        env:
          MEMFAULT_KEY: ${{ secrets.MEMFAULT_PROJECT_KEY }}
        run: |
          # Determine app path
          APP_PATH="${{ matrix.app.path }}"
          if [ -z "$APP_PATH" ]; then
            APP_PATH="."
            APP_PATH_DISPLAY="(root)"
          else
            APP_PATH_DISPLAY="$APP_PATH"
          fi
          
          echo "Building app at: $APP_PATH_DISPLAY"
          echo "Disk space available:"
          df -h | grep '/dev/root'
          
          docker run --rm \
            -v "$(pwd):/workdir" \
            -w /workdir \
            -e MEMFAULT_KEY="${MEMFAULT_KEY}" \
            -e APP_PATH="${APP_PATH}" \
            -e BUILD_NAME="${{ matrix.app.build_name }}" \
            ${{ env.DOCKER_IMAGE }} \
            bash -c '
              set -e
              export CMAKE_PREFIX_PATH=/opt/toolchains
              
              echo "============================================"
              echo "🚀 CI/CD Build Pipeline"
              echo "============================================"
              echo "📦 NCS Version: ${{ env.NCS_VERSION }}"
              echo "📋 Board: ${{ env.BOARD }}"
              echo "🏗️  App: ${{ matrix.app.name }}"
              echo "📂 Path: ${APP_PATH}"
              echo "============================================"
              
              cd /workdir/project
              
              # Initialize west
              echo ""
              echo "🔧 Initializing West workspace..."
              west init -l .
              
              # Update modules (or use cache)
              if [ "${{ steps.cache-west.outputs.cache-hit }}" != "true" ]; then
                echo "📦 Fetching NCS modules (not cached)..."
                west update -o=--depth=1 -n
                west blobs fetch hal_nordic
              else
                echo "✅ Using cached NCS modules"
              fi
              
              west zephyr-export
              
              # Install dependencies
              echo ""
              echo "📦 Installing Python dependencies..."
              pip3 install --no-cache-dir -q \
                -r ../nrf/scripts/requirements-build.txt \
                -r ../zephyr/scripts/requirements.txt \
                -r ../bootloader/mcuboot/scripts/requirements.txt
              
              # Set version
              if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
                VERSION="${{ github.ref_name }}"
              else
                VERSION="$(git rev-parse --short=8 HEAD)"
              fi
              echo "VERSION=$VERSION" > /workdir/version.txt
              echo "🏷️  Firmware Version: $VERSION"
              
              # Navigate to app directory
              cd "${APP_PATH}"
              
              # Create Memfault overlay
              echo ""
              echo "🔐 Configuring Memfault..."
              cat > overlay-memfault-ci.conf << "OVERLAY_EOF"
              CONFIG_MEMFAULT=y
              CONFIG_MEMFAULT_SHELL=y
              CONFIG_MEMFAULT_LOGGING_ENABLE=y
              CONFIG_MEMFAULT_LOG_LEVEL_INF=y
              CONFIG_MEMFAULT_NCS_BT_METRICS=y
              CONFIG_MEMFAULT_NCS_PROJECT_KEY="MEMFAULT_KEY_PLACEHOLDER"
              CONFIG_MEMFAULT_NCS_FW_VERSION_STATIC=y
              CONFIG_MEMFAULT_NCS_FW_VERSION="VERSION_PLACEHOLDER"
              CONFIG_MEMFAULT_NCS_FW_TYPE="ci-${BUILD_NAME}"
              CONFIG_MEMFAULT_NCS_DEVICE_ID_STATIC=y
              CONFIG_MEMFAULT_NCS_DEVICE_ID="ci-${BUILD_NAME}-dev01"
              CONFIG_HW_ID_LIBRARY=n
              CONFIG_BT_DIS=y
              CONFIG_BT_DIS_SERIAL_NUMBER=y
              CONFIG_BT_DIS_SERIAL_NUMBER_STR="ci-${BUILD_NAME}-dev01"
              CONFIG_BT_DIS_HW_REV=y
              CONFIG_BT_DIS_HW_REV_STR="nrf7002dk_nrf5340"
              CONFIG_BT_DIS_SW_REV=y
              CONFIG_BT_DIS_SW_REV_STR="main"
              CONFIG_BT_DIS_FW_REV=y
              CONFIG_BT_DIS_FW_REV_STR="VERSION_PLACEHOLDER"
              CONFIG_MCUMGR_GRP_OS_INFO=y
              CONFIG_BT_SMP=y
              CONFIG_BT_SETTINGS=y
              CONFIG_FLASH_MAP=y
              CONFIG_STREAM_FLASH=y
              CONFIG_IMG_MANAGER=y
              CONFIG_DFU_TARGET=y
              CONFIG_DFU_TARGET_MCUBOOT=y
              CONFIG_HEAP_MEM_POOL_SIZE=4096
              OVERLAY_EOF
                            
              # Substitute placeholders
              sed -i "s/MEMFAULT_KEY_PLACEHOLDER/${MEMFAULT_KEY}/g" overlay-memfault-ci.conf
              sed -i "s/VERSION_PLACEHOLDER/$VERSION/g" overlay-memfault-ci.conf
              
              echo "✅ Memfault overlay created"
              
              # Build
              echo ""
              echo "============================================"
              echo "🔨 Building ${{ matrix.app.name }}"
              echo "============================================"
              
              set +e
              west build \
                -b ${{ env.BOARD }} \
                -p always \
                --sysbuild \
                -- -DEXTRA_CONF_FILE=overlay-memfault-ci.conf \
                2>&1 | tee build.log
              
              BUILD_STATUS=$?
              set -e
              
              if [ $BUILD_STATUS -ne 0 ]; then
                echo ""
                echo "❌ BUILD FAILED with exit code $BUILD_STATUS"
                exit $BUILD_STATUS
              fi
              
              echo ""
              echo "✅ BUILD SUCCEEDED"
              
              # Process artifacts
              echo ""
              echo "📦 Processing artifacts..."
              cd build
              
              APP_DIR=$(find . -maxdepth 2 -type f -name "zephyr.elf" -print -quit | xargs dirname | xargs dirname)
              if [ -z "$APP_DIR" ]; then
                APP_DIR="."
              fi
              
              echo "📂 App build directory: $APP_DIR"
              
              # Memory reports
              if [ -f "$APP_DIR/zephyr/zephyr.elf" ]; then
                echo "📊 Memory usage (Application Core):"
                arm-zephyr-eabi-size "$APP_DIR/zephyr/zephyr.elf" | tee memory-app.txt
              fi
              
              if [ -f "ipc_radio/zephyr/zephyr.elf" ]; then
                echo "📊 Memory usage (Network Core):"
                arm-zephyr-eabi-size "ipc_radio/zephyr/zephyr.elf" | tee memory-net.txt
              fi
              
              # Rename artifacts
              VERSION=$(cat /workdir/version.txt)
              PREFIX="${BUILD_NAME}-$VERSION"
              
              [ -f "$APP_DIR/zephyr/zephyr.elf" ] && cp "$APP_DIR/zephyr/zephyr.elf" "${PREFIX}-app.elf"
              [ -f "$APP_DIR/zephyr/zephyr.hex" ] && cp "$APP_DIR/zephyr/zephyr.hex" "${PREFIX}-app.hex"
              [ -f "merged.hex" ] && cp "merged.hex" "${PREFIX}-merged.hex"
              [ -f "$APP_DIR/zephyr/zephyr.signed.bin" ] && cp "$APP_DIR/zephyr/zephyr.signed.bin" "${PREFIX}-ota.bin"
              [ -f "$APP_DIR/zephyr/app_update.bin" ] && cp "$APP_DIR/zephyr/app_update.bin" "${PREFIX}-update.bin"
              [ -f "dfu_application.zip" ] && cp "dfu_application.zip" "${PREFIX}-dfu.zip"
              [ -f "$APP_DIR/zephyr/.config" ] && cp "$APP_DIR/zephyr/.config" "${PREFIX}.config"
              
              echo "✅ Artifacts prepared"
              ls -lh ${PREFIX}-* 2>/dev/null || true
            '

      - name: 📤 Upload Build Logs (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: logs-${{ matrix.app.build_name }}-${{ github.run_number }}
          path: |
            project/${{ matrix.app.path != '' && format('{0}/', matrix.app.path) || '' }}build.log
            project/${{ matrix.app.path != '' && format('{0}/', matrix.app.path) || '' }}build/**/CMakeFiles/CMakeError.log
          retention-days: 7
          if-no-files-found: warn

      - name: 📦 Upload Firmware Artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.app.build_name }}-${{ github.run_number }}
          path: |
            project/${{ matrix.app.path != '' && format('{0}/', matrix.app.path) || '' }}build/${{ matrix.app.build_name }}-*
            project/${{ matrix.app.path != '' && format('{0}/', matrix.app.path) || '' }}build/memory-*.txt
            project/${{ matrix.app.path != '' && format('{0}/', matrix.app.path) || '' }}build.log
          retention-days: 30
          if-no-files-found: error

      - name: 📝 Build Summary
        if: always()
        run: |
          VERSION=$(cat version.txt 2>/dev/null || echo "${{ github.sha }}")
          cat >> $GITHUB_STEP_SUMMARY << EOF
          ## ${{ matrix.app.name }} - Build ${{ job.status }}
          
          | Property | Value |
          |----------|-------|
          | **Board** | ${{ env.BOARD }} |
          | **Version** | \`${VERSION}\` |
          | **NCS** | ${{ env.NCS_VERSION }} |
          | **Status** | **${{ job.status }}** |
          | **Artifact** | \`firmware-${{ matrix.app.build_name }}-${{ github.run_number }}\` |
          EOF
