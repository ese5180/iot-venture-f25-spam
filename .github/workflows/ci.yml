name: nRF7002DK CI/CD Pipeline (v3.0.2)

on:
  push:
    branches: [main, develop, master]
  pull_request:
    branches: [main, develop, master]
  workflow_dispatch:

jobs:
  build:
    name: Build Firmware (NCS v3.0.2)
    runs-on: ubuntu-latest

    strategy:
      matrix:
        app:
          - name: "Main BLE Application"
            path: "iot-venture-spam"
            build_name: "main-app"
          - name: "FOTA Application"
            path: "iot-venture-spam/src/subroutines/FOTA"
            build_name: "fota"
          - name: "Servo Application"
            path: "iot-venture-spam/src/subroutines/servo"
            build_name: "servo"

    container:
      image: ghcr.io/zephyrproject-rtos/ci:v0.26.13
      options: --cpus 2
      env:
        CMAKE_PREFIX_PATH: /opt/toolchains

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          path: project

      - name: ♻️ Initialize NCS v3.0.2 Workspace
        run: |
          west init -m https://github.com/nrfconnect/sdk-nrf --mr v3.0.2 ncs
          cd ncs
          west update --narrow -o=--depth=1
          west zephyr-export

      - name: 📦 Install Python Requirements
        run: |
          pip3 install -r ncs/zephyr/scripts/requirements.txt
          pip3 install -r ncs/nrf/scripts/requirements.txt
          pip3 install -r ncs/bootloader/mcuboot/scripts/requirements.txt

      - name: 📂 Copy Project to Workspace
        run: |
          mkdir -p ncs/iot-venture-spam
          cp -r project/* ncs/iot-venture-spam/

      - name: 🔨 Build ${{ matrix.app.name }}
        run: |
          cd ncs
          west build -b nrf7002dk/nrf5340/cpuapp --sysbuild ${{ matrix.app.path }} -p always

      - name: 📊 Generate Build Reports
        if: success()
        run: |
          cd ncs/build
          ninja rom_report ram_report

      - name: 📦 Upload Firmware Artifacts
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: firmware-${{ matrix.app.build_name }}-${{ github.sha }}
          path: |
            ncs/build/**/zephyr/*.elf
            ncs/build/**/zephyr/*.hex
            ncs/build/**/zephyr/*.bin
            ncs/build/**/zephyr/app_update.bin
            ncs/build/**/merged.hex
          retention-days: 30

      - name: 📈 Upload Memory Reports
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: memory-reports-${{ matrix.app.build_name }}-${{ github.sha }}
          path: |
            ncs/build/rom.json
            ncs/build/ram.json
          retention-days: 7

  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔍 Check for TODOs and FIXMEs
        run: |
          echo "Scanning for TODO/FIXME comments..."
          grep -rn "TODO\|FIXME" src/ || echo "No TODOs or FIXMEs found"

      - name: 📏 Check File Sizes
        run: |
          echo "Checking for large files..."
          find . -type f -size +1M -not -path "./.git/*" -not -path "./images/*"


